<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.aplikasiabsensi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplikasiAbsensi">
        <activity android:name=".view.ActivitySettings"></activity>
        <activity android:name=".view.AbsenSiswaActivity" />
        <activity android:name=".view.AktivasiAbsenActivity" />
        <activity android:name=".view.DetailPelajaranActivity" />
        <activity android:name=".view.PelajaranActivity" />
        <activity android:name=".view.AbsensiActivity" />
        <activity android:name=".view.QrCodeActivity" />
        <activity android:name=".view.ProfilActivity" />
        <activity android:name=".view.DashboardActivity" />
        <activity android:name=".view.LoginActivity" />
        <activity android:name=".view.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>